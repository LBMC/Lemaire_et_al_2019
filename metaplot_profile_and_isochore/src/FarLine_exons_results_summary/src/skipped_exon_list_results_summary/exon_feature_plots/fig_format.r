#!/usr/bin/Rscript

fig_format <- theme_bw( base_size = 16 ) + theme(legend.position="bottom") + theme(legend.title=element_blank()) + theme( legend.text=element_text( size=20 ) )
# + theme( axis.text.x = element_text( angle = 45, hjust = 1 ) )
